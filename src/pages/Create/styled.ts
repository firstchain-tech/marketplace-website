import styled from 'styled-components'
import { ComLayout } from '@/common/styled'

export const CreateWrapper = styled(ComLayout)`
  min-height: 80vh;
`

export const CreateWalletWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 80vh;
`
